// This is a mapping of Debian release codenames to processor architectures to kernel packages
{
"trusty": // In squeeze, we need a special kernel flavor for xen
	{"i386":  "linux-image-virtual",
	 "amd64": "linux-image-virtual"},
"precise": // In squeeze, we need a special kernel flavor for xen
	{"i386":  "linux-image-virtual",
	 "amd64": "linux-image-virtual"},
"utopic": // In squeeze, we need a special kernel flavor for xen
	{"i386":  "linux-image-virtual",
	 "amd64": "linux-image-virtual"},
"saucy": // In squeeze, we need a special kernel flavor for xen
	{"i386":  "linux-image-virtual",
	 "amd64": "linux-image-virtual"}
}
